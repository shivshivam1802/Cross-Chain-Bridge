{
  "name": "cross-chain-bridge-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Cross-Chain Bridge",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@chainlink/contracts-ccip": "^1.4.0",
    "@chainlink/local": "^0.2.1",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
